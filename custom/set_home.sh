#!/bin/bash

# make Directory my workspace
mkdir -v -p "${HOME}/Main.d"
mkdir -v -p  "${HOME}/Main.d/Document.d"
mkdir -v -p  "${HOME}/Main.d/Develop.d"
mkdir -v -p  "${HOME}/Main.d/Downloads.d"
mkdir -v -p  "${HOME}/Main.d/DustBox.d"